{
  "name": "example jwt",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "node index.js"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  }
}